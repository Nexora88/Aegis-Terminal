services:
  aegis:
    build: .
    container_name: aegis-terminal
    ports:
      - "8000:8000"
    environment:
      AEGIS_DATA_DIR: /app/data
      AEGIS_COLLECT_INTERVAL: 15
    volumes:
      - aegis_data:/app/data
    restart: unless-stopped

volumes:
  aegis_data:
